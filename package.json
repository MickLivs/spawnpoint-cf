{
  "name": "spawnpoint-cf",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "WRANGLER_DOCKER_BIN=./docker-shim.sh wrangler dev",
    "deploy": "WRANGLER_DOCKER_BIN=./docker-shim.sh wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/sandbox": "0.7.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
